# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Node / Frontend specific
client/node_modules
client/dist
client/dist-ssr

# Cypress test artifacts (root level if any)
cypress/screenshots/
cypress/videos/

# Rails / Backend specific
server/tmp/*
!server/tmp/cache
!server/tmp/cache/assets
!server/tmp/sockets
!server/tmp/pids
server/log/*
!server/log/.keep
server/storage/*
!server/storage/.keep
server/tmp/storage/ # Ignore Active Storage temp files for tests
server/public/assets
.bundle/
config/master.key

# Kamal secrets and potentially large build cache
.kamal/*
# But DO track the hooks directory
!.kamal/hooks/
# And ensure the specific hook file isn't ignored by other patterns (optional but safe)
!.kamal/hooks/pre-deploy
# Also specifically ignore the env file if it exists
.kamal/*.env

# Local backups (not committed)
/backups/
